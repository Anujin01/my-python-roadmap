# 📝Collection = single variable used to store multiple values 

## ✔️collection 1:

### List = [] ordered and changeable, duplicates ok.

fruits = ["apple", "coconut", "banana", "strawberry"]

print(dir(fruits))  this command shows everything this value in parenthesis can do

print(help(fruits)) "print(help(x))"" you can use this command to get the description of all the attributes, and methods

print(len(fruits))

## ✔️collection 2:

### Set = {} unordered and immutable, but add/remove is ok. no duplicates.

fruits = {"apple", "coconut", "banana", "strawberry"}

print(dir(fruits))   this command shows everything this value in parenthesis can do

print(help(fruits)) "print(help(x))"" you can use this command to get the description of all the attributes, and methods

print(len(fruits))

## ✔️collection 3:

### Tuple = () ordered and unchangeable, duplicates ok, faster

fruits = ("apple", "coconut", "banana", "strawberry")

print(dir(fruits))   this command shows everything this value in parenthesis can do

print(help(fruits)) "print(help(x))"" you can use this command to get the description of all the attributes, and methods

print(len(fruits))
